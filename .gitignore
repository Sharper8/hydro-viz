# Données brutes et intermédiaires : régénérées par le pipeline (scripts/)
data/raw/
data/prepared/

# Node / Vite
node_modules/
web/dist/
*.log

# Env local (GLITCHTIP_DSN injecté au build via secret GitHub, jamais commité)
.env
.env.local
web/.env
web/.env.local

# Divers
.DS_Store
.venv/
__pycache__/
